server.port=8080

# MySQL Database
spring.datasource.url=jdbc:mysql://localhost:3306/lensdock?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Kolkata
spring.datasource.username=lensdock
spring.datasource.password=Sudu@1308
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=false

# JWT Secret (256-bit minimum for HS256)
app.jwt.secret=LensDockSecureJWT2026KolhapurCameraRentalBase64EncodedSigningKey
app.jwt.expiration-ms=86400000

# Max upload size for Aadhaar / ID card (base64 stored in DB)
spring.servlet.multipart.max-file-size=1MB
spring.servlet.multipart.max-request-size=1MB

# Logging
logging.level.com.lensdock=INFO
logging.level.org.springframework.security=WARN

# ── Gmail SMTP (LensDock email notifications) ─────────────────────────────────
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=lensdock.team@gmail.com
spring.mail.password=pquxfacgwvixryrg
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.default-encoding=UTF-8

# Daily email limit
app.email.daily-limit=300
